\chapter{Simulation and Stability Analysis}
\label{chap:simulation}

This chapter studies the behaviour of the trained neural encoder--decoder model when it is used for simulation. Whereas the previous chapters focused on reconstructing observed swap curves, the purpose here is to examine whether the learned model can also generate stable future yield-curve scenarios. This requires simulating the latent state forward in time and decoding each simulated state into a full zero-coupon bond curve.

The experiment addresses an important practical question: is accurate reconstruction of observed curves sufficient for using the model as a scenario generator? To investigate this, we compare two model variants. The first is the baseline model, whose latent dynamics are learned without explicit stability constraints. The second is a stable parameterisation designed to produce better-behaved simulated paths. The comparison focuses on whether the simulated latent states remain in regions where the decoder is meaningful, and whether the resulting short rates and zero-coupon bond curves remain economically interpretable over the simulation horizon.

\textcolor{red}{
This chapter studies the behaviour of the trained neural encoder--decoder model when it is used beyond static curve reconstruction. The model is designed to map observed swap curves into a low-dimensional latent representation and to decode latent states into arbitrage-free zero-coupon bond curves. A natural next question is therefore whether the learned latent dynamics can be used to generate stable yield-curve scenarios.
}

\textcolor{red}{
This is not guaranteed by reconstruction accuracy alone. A model may reproduce observed swap curves well, while the simulated latent process may still drift away from the region represented in the training data. If this happens, the decoder, volatility network and short-rate network are evaluated outside the region where they were trained, and the resulting curves may become unstable or economically difficult to interpret.
}

\textcolor{red}{
The chapter therefore examines simulation stability by comparing two model variants. The first is the baseline model, whose latent dynamics are learned without explicit stability constraints. The second is a stable parameterisation designed to improve the behaviour of simulated paths. The comparison focuses on whether the latent states remain in meaningful regions and whether the resulting short rates and zero-coupon bond curves remain well behaved over the simulation horizon.
}

%========================================================
\section{Simulation of Latent Dynamics and Curve Reconstruction}
\label{sec:simulation-latent-dynamics}
%========================================================
We use the same swap-rate data as in the previous chapters. Let \(S_0 \in \mathbb{R}^8\) denote the observed market swap curve selected as the initial condition for the simulation, corresponding to the eight quoted maturities used in the model.
The trained encoder maps the initial swap curve to a latent initial state,
\[
z_0 = E(S_0) \in \mathbb{R}^{\ell},
\]
where \(\ell\) denotes the latent dimension. Starting from \(z_0\), the latent state is simulated forward under the learned risk-neutral dynamics
\[
dz_t = \mu(z_t)\,dt + \sigma(z_t)\,dW_t^{\mathbb{Q}},
\]
where \(W_t^{\mathbb{Q}}\) is an \(\ell\)-dimensional Brownian motion under the risk-neutral measure \(\mathbb{Q}\). The simulated dynamics are therefore interpreted as risk-neutral dynamics generated by the trained model, rather than as historical real-world dynamics estimated from observed time-series changes.
In the implementation, the drift and short-rate functions are given by the trained networks
\[
\mu(z)=\mathcal{K}(z),
\qquad
\tilde r(z)=\mathcal{R}(z).
\]
The network \(\mathcal{H}\) returns state-dependent parameters from which the volatility levels and correlations are constructed. These quantities are then assembled into the volatility loading matrix \(\sigma(z)\), constructed as the lower-triangular Cholesky factor of the instantaneous covariance matrix as described in Chapter~\ref{chap:encoderdecodermodel}. This matrix enters directly in the latent simulation.
In the two-dimensional case, this corresponds to writing
\[
\sigma(z)
=
\begin{pmatrix}
\sigma_1(z) & 0 \\
\rho(z)\sigma_2(z) & \sqrt{1-\rho(z)^2}\,\sigma_2(z)
\end{pmatrix},
\]
where \(\sigma_1(z)\) and \(\sigma_2(z)\) are the instantaneous volatility levels of the two latent factors, and \(\rho(z)\) is the instantaneous correlation between their stochastic innovations.
%where \(\sigma_1(z)\) and \(\sigma_2(z)\) are the instantaneous volatility levels of the two latent factors, and \(\rho(z)\) determines the instantaneous correlation between the random changes in the two latent factors.
The latent dynamics are discretised using the Euler--Maruyama scheme. For a time step \(\Delta t>0\) and grid points \(t_n=n\Delta t\), the simulated paths satisfy
\[
z_{t_{n+1}}
=
z_{t_n}
+
\mu(z_{t_n})\,\Delta t
+
\sigma(z_{t_n})\,\Delta W_n,
\qquad
\Delta W_n \sim \mathcal{N}(0,\Delta t\,I_{\ell}).
\]
From the simulated latent path we obtain the corresponding short-rate path
\[
r_{t_n}=\tilde r(z_{t_n}),
\qquad n=0,\ldots,N.
\]
The associated bank account is
\[
B_t=\exp\!\left(\int_0^t r_s\,ds\right),
\]
and the pathwise discount factor is therefore
\[
D_t=B_t^{-1}
=
\exp\!\left(-\int_0^t r_s\,ds\right).
\]
Since the short rate is only observed on the simulation grid, the integral is approximated numerically using the trapezoidal rule,
\[
D_{t_n}
\approx
\exp\!\left(
-
\sum_{k=0}^{n-1}
\frac{r_{t_k}+r_{t_{k+1}}}{2}\,\Delta t
\right).
\]

For each simulated latent state \(z_{t_n}\), the corresponding zero-coupon bond curve is obtained through the decoder described in Chapter~\ref{chap:encoderdecodermodel}. Conditional on this state, the functions \(A_{z_{t_n}}(\tau)\) and \(B_{z_{t_n}}(\tau)\) are obtained by solving the no-arbitrage ODE system numerically, and bond prices are recovered from
\[
P(z_{t_n},\tau)
=
\exp\bigl(
A_{z_{t_n}}(\tau)
-
B_{z_{t_n}}(\tau)\mathcal{G}(z_{t_n},\tau)
\bigr).
\]

Hence, each simulated path produces a latent-factor path, a short-rate path, pathwise discount factors, and a sequence of zero-coupon bond curves across maturities. These objects form the basis for assessing whether the trained model remains stable when it is used as a scenario generator.


%========================================================
\section{Stability Analysis of the Simulation Experiment}
\label{sec:stability_analysis}
%========================================================

The simulation procedure turns the trained encoder--decoder model into a forward scenario generator. Starting from an observed swap curve, the model first maps the curve to a latent initial state, then simulates the latent state forward in time, and finally decodes each simulated state into a zero-coupon bond curve. This requires more than accurate reconstruction of observed swap curves. The simulated latent paths must also remain stable over the simulation horizon, since unstable latent dynamics may lead to short rates and bond curves that are economically difficult to interpret. 

This section compares the baseline model with a stable parameterisation. The baseline model has an unconstrained affine drift and therefore does not guarantee mean reversion. In contrast, the stable model introduces a Hurwitz drift, bounded volatilities, positive-definite correlation matrices and a bounded short-rate parameterisation by construction.

%========================================================
\subsection{Well-Posedness and Simulation Stability}
\label{subsec:wellposedness_stability}
%========================================================

The latent simulation model is an SDE of the form
\[
dz_t = \mu(z_t)\,dt + \sigma(z_t)\,dW_t^{\mathbb{Q}}.
\]
As discussed in Proposition~\ref{prop:sde_existence}, Lipschitz continuity and linear growth of the drift and diffusion coefficients are sufficient for the existence of a unique non-explosive solution.

In the numerical experiments in this chapter we consider the two-dimensional case \(\ell=2\). For the baseline model, the drift is affine and therefore globally Lipschitz. The volatility network \(\mathcal{H}\) uses bounded activation functions, so for fixed trained weights its raw outputs remain bounded. After applying \textcolor{red}{the exponential and hyperbolic tangent transformations}, the resulting volatility levels are positive and bounded, while the correlation parameter lies in \((-1,1)\). In two dimensions, this is sufficient to produce a valid \(2\times2\) correlation matrix. The stable model is constructed to satisfy the same type of regularity conditions.

%After applying the transformations
%\[ \sigma_i(z)=\exp(u_i(z)), \qquad \rho(z)=\tanh(v(z)),\]
%the resulting volatility levels are positive and bounded, while the correlation parameter lies in \((-1,1)\).

However, mathematical well-posedness is weaker than practical simulation stability. A process may have a unique finite-time solution while still moving far outside the region covered by the encoded training data. In that case, the decoder, volatility network and short-rate network are forced to extrapolate beyond the latent states on which they were trained. The resulting short rates and zero-coupon bond curves may therefore be mathematically defined, but economically difficult to interpret.

The analysis below therefore examines whether the simulated paths remain stable enough to produce plausible model-generated yield-curve scenarios.


%========================================================
\subsection{Baseline Failure Modes}
\label{subsec:sim_baseline_problems}
%========================================================

When the baseline model is used for forward simulation, three related failure modes can arise. They all stem from the fact that the reconstruction objective does not by itself constrain the behaviour of the learned functions away from the encoded training data.

Before discussing the drift instability, we recall the following definition.

\begin{definition}[Hurwitz matrix]
A square matrix \(M\in\mathbb{R}^{\ell\times\ell}\) is called Hurwitz if all
eigenvalues of \(M\) have strictly negative real parts, i.e.
\[
\operatorname{Re}(\lambda_i(M))<0,
\qquad i=1,\ldots,\ell.
\]
\end{definition}

Here \(\operatorname{Re}(\lambda_i(M))\) denotes the real part of the eigenvalue \(\lambda_i(M)\). This is relevant because a real matrix may have complex eigenvalues.

\begin{description}

\item[Unstable drift.]
The baseline drift is given by
\[
\mathcal{K}(z)=Mz+N,
\qquad
M\in\mathbb{R}^{\ell\times\ell},
\quad
N\in\mathbb{R}^{\ell},
\]
where \(M\) and \(N\) are learned parameters. The matrix \(M\) determines how the current latent state affects the deterministic drift of the process. Since \(M\) is learned without restrictions on its eigenvalues, the baseline model does not guarantee mean reversion.

To see this, consider the deterministic part of the latent dynamics,
\[
\dot z_t=Mz_t+N,
\]
where we temporarily omit the Brownian shock term in order to study the stability of the deterministic drift. If \(M\) is invertible, the fixed point \(z^*\) is the state where the deterministic drift is zero:
\[
Mz^*+N=0,
\qquad
z^*=-M^{-1}N.
\]
Writing the deviation from this fixed point as \(z_t-z^*\), we obtain
\[
z_t-z^*=e^{Mt}(z_0-z^*),
\]
where \(e^{Mt}\) denotes the matrix exponential. Thus the eigenvalues of \(M\) determine whether deviations from \(z^*\) decay or grow over time.

Mean reversion toward \(z^*\) requires all eigenvalues of \(M\) to have strictly negative real parts, i.e. \(M\) must be Hurwitz. If \(M\) has an eigenvalue with non-negative real part, then deviations in the corresponding latent direction do not decay. In a stochastic simulation, random shocks are then no longer systematically pulled back toward the fixed point and may instead be propagated along unstable latent directions, causing the latent state to move far away from the region covered by the encoded training data.

\item[Short-rate saturation.]
With zero bias terms, the baseline short-rate network has the form
\[
\tilde r(z)=W_2\psi(W_1z),
\qquad
\psi(x)=\frac{1}{1+e^{-x}}-\frac12.
\]
Since each component of \(\psi(W_1z)\) lies in \((-1/2,1/2)\), the output satisfies
\[
|\tilde r(z)|
=
|W_2\psi(W_1z)|
\leq
\sum_j |(W_2)_j|\,|\psi((W_1z)_j)|
\leq
\frac12\|W_2\|_1
\qquad
\forall z.
\]

Thus the short rate remains bounded for all latent states. However, if the latent state becomes very large, then \(W_1z\) becomes large in magnitude and the centered soft-step activation is pushed close to its bounds \(\pm 1/2\). The derivative of the activation is then close to zero, so the short-rate network becomes almost insensitive to further changes in \(z\). The simulated short rate is therefore pinned near an artificial network-implied upper or lower level. It remains finite, but this boundedness is a saturation effect rather than evidence of stable dynamics.

\item[Decoder extrapolation.]
Once the latent state leaves the region covered by the encoded training data, the decoder \(\mathcal{G}\) is evaluated far away from the states on which it was trained. The no-arbitrage ODE system still produces zero-coupon bond prices, so the output remains mathematically well defined. However, the shape of the resulting bond curve is then driven by extrapolation of the neural network rather than by behaviour learned from observed market curves. The resulting bond prices and swap rates may therefore be economically difficult to interpret, even if the model reconstructs the training data accurately.

\end{description}

%========================================================
\subsection{Stable Parameterisation}
\label{subsec:sim_stable_solutions}
%========================================================

The failure modes described above motivate a stable variant of the model. The baseline and stable models share the same overall encoder--decoder framework and the same arbitrage-free bond-pricing construction. They differ only in the parameterisations of the drift network \(\mathcal{K}\), the diffusion network \(\mathcal{H}\), and the short-rate network \(\mathcal{R}\). The purpose of the stable parameterisation is to restrict the learned dynamics so that forward simulations remain better behaved.

\begin{description}

\item[Hurwitz drift.]
The stable model replaces the unconstrained drift matrix with
\[
M
=
-\left(V^\top V+\varepsilon I_\ell\right),
\qquad
V\in\mathbb{R}^{\ell\times\ell},
\quad
\varepsilon>0,
\]
where \(V\) is a trainable matrix and \(\varepsilon\) is a fixed small positive constant. For any vector \(x\in\mathbb{R}^{\ell}\),
\[
x^\top V^\top Vx
=
(Vx)^\top(Vx)
=
\|Vx\|^2
\geq 0.
\]
Hence \(V^\top V\) is positive semi-definite. Since \(\varepsilon I_\ell\) is strictly positive definite, the matrix \(V^\top V+\varepsilon I_\ell\) is strictly positive definite. Therefore all eigenvalues of
\[
M=-\left(V^\top V+\varepsilon I_\ell\right)
\]
are strictly negative, with
\[
\lambda_i(M)\leq -\varepsilon<0.
\]
The drift matrix is therefore Hurwitz by construction. Since \(M\) is invertible, the deterministic drift has the equilibrium
\[
z^*=-M^{-1}N,
\]
and the latent factors are pulled back toward this point.

This parameterisation is sufficient but not necessary for mean reversion. A general Hurwitz matrix need not be symmetric, whereas the matrix above is symmetric negative definite. The restriction is nevertheless useful because it guarantees stable deterministic drift for any value of the trainable matrix \(V\).

\item[Bounded volatilities.]
The volatility levels enter the diffusion matrix \(\sigma(z)\) and therefore determine the scale of the random shocks in the latent simulation. In the baseline parameterisation, individual volatilities are recovered as
\[
\sigma_i(z)=\exp([\mathcal{H}(z)]_i).
\]
This ensures positivity. Moreover, for fixed trained weights and bounded activation functions, the network output is bounded. However, the resulting upper bound is implicit and depends on the learned parameters. Consequently, the baseline model does not provide an explicit numerical control on the diffusion scale when the latent state is evaluated away from the training region.

The stable model instead uses the sigmoid-bounded parameterisation
\[
\log\sigma_i(z)
=
\log\sigma_{\min}
+
\bigl(\log\sigma_{\max}-\log\sigma_{\min}\bigr)
\operatorname{sigmoid}\bigl(h_i(z)+b_i\bigr),
\]
where \(h_i(z)\) is the \(i\)-th network output before the volatility transformation, \(b_i\) is a learnable offset, and
\[
0<\sigma_{\min}<\sigma_{\max}
\]
are prespecified numerical bounds. Since the sigmoid takes values in \((0,1)\), this ensures
\[
\sigma_i(z)\in(\sigma_{\min},\sigma_{\max})
\qquad
\forall z\in\mathbb{R}^{\ell}.
\]
The diffusion scale therefore remains explicitly controlled even when the latent state is outside the region observed during training.

\item[Positive-definite correlations.]
The baseline network parameterises pairwise correlations as
\[
\rho_{ij}(z)=\tanh([\mathcal{H}(z)]_k),
\]
which ensures that each individual correlation lies in \((-1,1)\). For \(\ell=2\), this is sufficient to define a valid \(2\times2\) correlation matrix. For \(\ell\geq3\), however, specifying each pairwise correlation separately does not in general guarantee that the full correlation matrix is positive definite. Thus, in the two-dimensional simulations below, this modification \textcolor{red}{( the hyperspherical construction)} is not needed for validity of the correlation matrix, but it becomes relevant in the general multi-factor formulation.

The stable model instead constructs the correlation matrix through a hyperspherical Cholesky parameterisation. Rather than predicting the pairwise correlations directly, the network outputs \(\ell(\ell-1)/2\) unconstrained values \(g_k(z)\), which are transformed into angles
\[
\theta_k(z)=\pi\,\operatorname{sigmoid}(g_k(z)).
\]
Hence \(\theta_k(z)\in(0,\pi)\) for all \(z\). These angles are used to construct a lower-triangular matrix \(L_R(z)\) with unit row norms and strictly positive diagonal entries, as described in Appendix~\ref{app:hyperspherical_cholesky}. The correlation matrix is then defined by
\[
R(z)=L_R(z)L_R(z)^\top.
\]
Since \(L_R(z)\) has full rank, \(R(z)\) is positive definite. Since the rows of \(L_R(z)\) have unit norm, the diagonal entries of \(R(z)\) are equal to one. Thus \(R(z)\) is a valid correlation matrix by construction.

\item[Bounded short-rate network.]
The short-rate network is also modified in the stable variant. Recall that the baseline short-rate network is bounded because of the centered soft-step activation, but its effective upper and lower limits are determined indirectly by the learned weights. If the latent state becomes large, the activation saturates and the short rate can be pushed to these network-implied limits.

The stable model makes the bounded range explicit by using
\[
r(z)
=
r_{\mathrm{center}}
+
r_{\mathrm{scale}}\tanh(f(z)),
\]
where \(f(z)\) is a neural network, \(r_{\mathrm{center}}\) is a learnable centre parameter, and \(r_{\mathrm{scale}}>0\) is a learnable scale parameter. In the implementation, positivity of the scale is enforced by
\[
r_{\mathrm{scale}}
=
\operatorname{softplus}(\eta)+r_{\min},
\]
where \(\eta\) is an unconstrained trainable parameter and \(r_{\min}>0\) is a small lower bound. Since \(\tanh(f(z))\in(-1,1)\), the short rate satisfies
\[
r(z)
\in
\bigl(
r_{\mathrm{center}}-r_{\mathrm{scale}},
\;
r_{\mathrm{center}}+r_{\mathrm{scale}}
\bigr).
\]
Thus the stable short-rate network does not remove boundedness; instead, it makes the bound explicit and learnable. This avoids the situation where simulated rates are determined only by the accidental saturation limits of the baseline network.

\end{description}

Together, these modifications make the stable model more restrictive than the baseline model, but better suited for forward simulation. The Hurwitz drift controls the deterministic latent dynamics, the volatility functions remain within prespecified numerical bounds, the correlation matrix is positive definite by construction, and the short rate remains within a learnable bounded range. The empirical comparison below shows the practical effect of these changes on simulated paths.

The volatility bounds should be interpreted as stabilising hyperparameters rather than estimates of hard economic limits. They are chosen to be wide enough to contain the volatility values produced on the encoded training region, while still preventing extreme diffusion scales when the model is evaluated outside that region.

%========================================================
\subsection{Empirical Evidence from Simulated Paths}
\label{subsec:sim_evidence}
%========================================================

To demonstrate the practical effect of the stable parameterisation, we compare simulations generated from two trained models: the baseline model and the stable model, both using parameter values obtained after 5000 training epochs. Both models are initialized from the same observed EUR swap curve, using the first available date in the dataset. Both variants use the same encoder architecture, but the encoder weights are learned separately as part of each trained model. Hence, the same initial swap curve may be mapped to different latent initial states in the two checkpoints. The latent dynamics are then simulated using identical Brownian increments. This paired simulation design reduces Monte Carlo noise and makes the comparison primarily reflect the effect of the model parameterisation. All figures use \(N=500\) simulated paths, monthly time steps \(\Delta t=1/12\), a 10-year horizon and random seed 1234.

\paragraph{Root cause: drift-matrix eigenvalues.}

The fundamental difference between the two variants is visible in the eigenvalues of the learned drift matrix \(M\), shown in Figure~\ref{fig:drift_eigenvalues}. Both eigenvalues of the baseline matrix are positive,
\[
\lambda_1 \approx 5.571,
\qquad
\lambda_2 \approx 0.120.
\]
Thus, deviations from the deterministic equilibrium are amplified rather than dampened. This explains why the baseline latent dynamics can move away from the region covered by the encoded training data during simulation. In contrast, both eigenvalues of the stable matrix are strictly negative,
\[
\lambda_1 \approx -1.327,
\qquad
\lambda_2 \approx -10.846,
\]
which implies mean reversion toward the equilibrium \(z^*\) in the deterministic part of the latent dynamics.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.65\textwidth]{Figures/Simulation/fig_eigenvalues.png}
    \caption{Eigenvalues of the learned drift matrix \(M\) for the baseline
    and stable variants (\(\ell=2\), 5000 training epochs). The baseline has
    two positive eigenvalues, while the stable parameterisation guarantees
    strictly negative eigenvalues.}
    \label{fig:drift_eigenvalues}
\end{figure}

\paragraph{Individual latent paths.}

Figure~\ref{fig:comp_latent_paths} shows individual simulated paths for both latent factors. The top row shows the baseline model. The paths appear almost flat for most of the horizon because the vertical axis must accommodate the extreme values reached near the end of the simulation. The figure should therefore be read primarily as a scale diagnostic: the baseline latent state eventually leaves the training region by many orders of magnitude. The bottom row shows the stable model. Some stable paths move outside the empirical \(\pm2\sigma\) training band, but this band is only a visual reference for the encoded training cloud and not a hard admissible region. The important point is that the stable paths remain on a comparable latent scale and do not diverge.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Figures/Simulation/fig1_latent_paths.png}
    \caption{Individual Euler--Maruyama paths for \(z_1(t)\) and \(z_2(t)\)
    over 10 years. Top row: baseline. Bottom row: stable. The baseline paths
    eventually leave the training scale by many orders of magnitude, while the
    stable paths remain on a comparable latent scale.}
    \label{fig:comp_latent_paths}
\end{figure}

\paragraph{Rate of divergence.}

Figure~\ref{fig:log_growth} shows the growth rate of the simulated latent factors by plotting
\(\log_{10}(\operatorname{mean}|z_d(t)|)\) over time for each dimension \(d\). This diagnostic is useful because exponential growth appears approximately linear on a logarithmic scale. Hence, if the baseline paths grow like \(e^{ct}\), then \(\log_{10}(\operatorname{mean}|z_d(t)|)\) should increase roughly linearly in time.

This is exactly what is observed for the baseline model, consistent with the positive eigenvalues of the learned drift matrix. The stable model behaves differently: after an initial widening due to stochastic shocks, the curves flatten and remain on a controlled scale. This indicates that the Hurwitz drift prevents systematic growth of the latent state.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Figures/Simulation/fig6_log_growth.png}
    \caption{Mean absolute latent factor values on a \(\log_{10}\) scale.
    A roughly linear increase on this scale corresponds to exponential growth
    in the original latent variables. The baseline model exhibits this pattern,
    while the stable model remains on a controlled scale.}
    \label{fig:log_growth}
\end{figure}

\paragraph{Percentile fan charts for the latent factors.}

Figure~\ref{fig:comp_fans} summarizes the cross-sectional distribution of the simulated latent paths over time. At each time point, the bands are computed from the 500 simulated paths. The inner band shows the 25--75 percentile range, while the outer band shows the 5--95 percentile range. Hence, the figure shows whether the instability is a general feature of the simulated distribution, rather than being caused by only a few extreme paths.

For the baseline model, the percentile bands widen rapidly and eventually move far outside the scale of the encoded training data. This shows that the divergence is not limited to isolated paths, but affects a large part of the simulated distribution. For the stable model, the bands widen initially and then remain on a controlled scale, which is consistent with mean-reverting latent dynamics under state-dependent volatility.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Figures/Simulation/fig4_percentile_fans.png}
    \caption{Percentile fan charts for \(z_1(t)\) and \(z_2(t)\). The bands
    summarize the distribution across 500 simulated paths at each time point.
    The outer bands show the 5--95 percentile range, while the inner bands show
    the 25--75 percentile range. Top row: baseline. Bottom row: stable.}
    \label{fig:comp_fans}
\end{figure}

\paragraph{Short-rate paths and fan charts.}

Figure~\ref{fig:comp_short_rate_paths} shows individual short-rate paths. The baseline short rate does not diverge numerically, because the short-rate network is bounded by the centered soft-step activation. Instead, once the latent state becomes very large, the activation saturates and the short rate is driven to the effective output limits of the trained network. In this experiment, these limits are approximately \(\pm 123\%\) when the short rate is expressed in annual percentage units. The stable short-rate paths remain within a much narrower learned range, approximately \([-11\%,11\%]\). Although this range is still wide, the important point is that the stable paths remain controlled and do not collapse to the artificial saturation pattern observed in the baseline model.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Figures/Simulation/fig2_short_rate.png}
    \caption{Short-rate paths \(r(t)\) for the baseline and stable models,
    each shown on its own scale. The baseline short rate is driven to the
    effective output limits of the centered soft-step network. The stable short
    rate remains within a learned bounded range and does not exhibit the same
    saturation pattern.}
    \label{fig:comp_short_rate_paths}
\end{figure}

Figure~\ref{fig:comp_short_rate_fan} shows the corresponding percentile fan charts. At each time point, the bands summarize the distribution of short rates across the 500 simulated paths. The baseline fan rapidly expands to the network-implied saturation levels and then remains almost flat, showing that a large fraction of paths have been pushed to the output limits. The stable fan widens initially but remains within its learned bounded range throughout the simulation horizon.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Figures/Simulation/fig5_short_rate_fan.png}
    \caption{Short-rate percentile fan charts for the baseline and stable
    models. The bands summarize the distribution across simulated paths at each
    time point. The baseline fan concentrates near the network-implied output
    limits, while the stable fan remains within a learned bounded range.}
    \label{fig:comp_short_rate_fan}
\end{figure}

\paragraph{Terminal distributions.}

Figure~\ref{fig:comp_terminal} compares the terminal distributions, meaning the distribution of simulated values across paths at the final simulation time \(t=10\) years. The stable terminal latent states remain on the scale of the encoded training cloud. The baseline terminal latent states, by contrast, are far outside this scale and cannot be displayed meaningfully on the same scatter plot; their magnitude is instead reported in Table~\ref{tab:sim_comparison_summary}. 

The terminal short-rate histograms show the same distinction. The stable model produces a broad but finite distribution within its learned short-rate range, whereas the baseline model produces a bimodal distribution concentrated near the saturation limits of the short-rate network.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Figures/Simulation/fig3_terminal_distributions.png}
    \caption{Terminal distributions at \(t=10\) years. Left: stable terminal
    latent-state scatter with training-support shading. Centre: stable terminal
    short-rate histogram. Right: baseline terminal short-rate histogram,
    showing concentration near the saturation limits. The baseline terminal
    latent states are off scale and are summarized numerically in
    Table~\ref{tab:sim_comparison_summary}.}
    \label{fig:comp_terminal}
\end{figure}

\paragraph{Volatility bounds.}

Figure~\ref{fig:sigma_bounds} compares the individual volatility functions \(\sigma_i(z)\), evaluated both on the encoded training cloud and on an expanded grid around the training region. This diagnostic does not describe simulated paths directly; rather, it illustrates how the learned volatility functions behave when evaluated away from the latent states observed during training.

On the expanded grid, the baseline volatility functions can take values substantially outside the range observed on the training cloud. The stable volatility functions remain within the prescribed interval \((\sigma_{\min},\sigma_{\max})\) by construction. The figure therefore illustrates the role of the bounded volatility parameterisation as a safeguard against extreme diffusion scales during extrapolation.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Figures/Simulation/fig_sigma_bounds.png}
    \caption{Distribution of \(\sigma_i(z)\) for the baseline and stable
    models, evaluated on the encoded training cloud and on an expanded grid
    around the training region. The diagnostic shows that the stable volatility
    functions respect the prescribed bounds \(\sigma_{\min}\) and
    \(\sigma_{\max}\), while the baseline model has no explicit numerical bound
    on the diffusion scale.}
    \label{fig:sigma_bounds}
\end{figure}


%========================================================
\subsection{Simulation Diagnostics}
\label{subsec:sim_summary}
%========================================================

Table~\ref{tab:sim_comparison_summary} summarises the key diagnostics for the paired simulation.

\begin{table}[H]
\centering
\caption{Simulation diagnostics for baseline and stable models
(\(\ell=2\), 5000 training epochs, EUR, \(N=500\) paths, \(T=10\) years,
\(\Delta t=1/12\)). Short-rate values are expressed as percentages.}
\label{tab:sim_comparison_summary}
\begin{tabular}{@{}lrr@{}}
\toprule
\textbf{Metric} & \textbf{Baseline} & \textbf{Stable} \\
\midrule
Paths finite & \(100.0\%\) & \(100.0\%\) \\
Maximum \(|z|\) over all paths & \(2.72\times10^{20}\) & \(3.51\) \\
\midrule
\(\operatorname{Re}(\lambda_1)\) of \(M\) & \(+5.571\) & \(-1.327\) \\
\(\operatorname{Re}(\lambda_2)\) of \(M\) & \(+0.120\) & \(-10.846\) \\
\midrule
Terminal \(z_1\) std & \(3.30\times10^{19}\) & \(0.668\) \\
Terminal \(z_2\) std & \(3.03\times10^{19}\) & \(0.107\) \\
\midrule
Terminal \(r\) mean (\%) & \(37.97\) & \(2.30\) \\
Terminal \(r\) std (\%)  & \(117.29\) & \(5.82\) \\
\(r\) range (\%)         & \([-123.3,\;123.3]\) & \([-11.1,\;11.1]\) \\
\midrule
\(\sigma_1\) range, training cloud   & \([0.5101,\;1.0873]\) & \([0.0288,\;1.7269]\) \\
\(\sigma_2\) range, training cloud   & \([0.4847,\;1.0975]\) & \([0.2598,\;0.3483]\) \\
\(\sigma_1\) range, expanded grid    & \([0.1563,\;4.4862]\) & \([0.0003,\;1.9949]\) \\
\(\sigma_2\) range, expanded grid    & \([0.1384,\;4.9840]\) & \([0.1596,\;0.4955]\) \\
\bottomrule
\end{tabular}
\end{table}

The diagnostics confirm what the figures show. The baseline model remains finite in floating-point arithmetic, but it is not stable as a forward scenario generator in this experiment. The latent state reaches values of order \(10^{20}\), the drift matrix has positive eigenvalues, and the short rate saturates at the network output limits. The stable model keeps the latent state on a controlled scale, has strictly negative drift eigenvalues, and produces short-rate paths that remain bounded without collapsing to the saturation pattern observed in the baseline model.

The central conclusion is that the structural constraints of the stable parameterisation --- Hurwitz drift, sigmoid-bounded volatilities, positive-definite correlations and a learnable bounded short-rate network --- are not merely cosmetic changes. They materially affect the behaviour of simulated paths. Good reconstruction accuracy is therefore not sufficient on its own: a model intended for simulation must also impose enough structure on the latent dynamics to generate stable and economically interpretable scenarios.

